# Based on Ubuntu
############################################################

# Set the base image to Ubuntu
FROM ubuntu

# File Author / Maintainer
MAINTAINER Rene

# Install Battbot

# Install necessary tools
RUN apt-get update && apt-get install -y nano curl dialog net-tools
RUN apt-get update && apt-get zeromq

# Copy a tar.gz file from the current directory
# It is automaticly extracted by docker
ADD archup.tar.gz /

## Start the server
## battarchiver -client="batt:7171" -addr="arch" -backupport="8282"
ENTRYPOINT ["./battarchiver"] 
